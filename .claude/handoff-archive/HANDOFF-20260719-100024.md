# Session Handoff

> Maintained by Claude during work sessions. Any fresh session should be able to
> read this file and continue with zero re-explanation. Keep it under ~150 lines —
> move finished history to the archive, don't let it grow forever.

**Last updated:** (date / task)

## Goal

One or two sentences: what we're building and why. Link the spec/issue if one exists.

## Current state

### Done
- (completed item — include file paths touched, e.g. `src/auth/login.ts`)

### In progress
- (exactly where work stopped — be surgical: "wired up the retry loop in
  `src/api/client.ts:fetchWithRetry`, but the backoff test still fails on the
  timeout case")

## Key decisions & why

<!-- The most valuable section. This is the expensive-to-rediscover knowledge
     that compaction and fresh sessions lose. -->
- Chose X over Y because... (date)

## Gotchas discovered

- (environment quirks, hidden dependencies, "the test suite needs FOO=1",
  "the mock server lives in tests/mocks/ and must be started first")

## Next steps (in order)

1. (specific, actionable — a fresh session should be able to start on #1 immediately)
2. ...

## Dead ends — do NOT retry

- (approaches already tried and abandoned, and why they failed)
